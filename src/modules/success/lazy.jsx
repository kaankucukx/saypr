import { lazy } from 'react';

/**
 * Field Container - Manages Formik fields and displays error message.
 *
 * @type {React.LazyExoticComponent<React.ComponentType<any>>}
 */
export const SuccesPage = lazy(() => import('.'))
